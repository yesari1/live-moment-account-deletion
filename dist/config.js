// Replace this value with the address that receives Live Moment deletion requests.
// Example: "privacy@yourdomain.com"
window.LIVE_MOMENT_CONFIG = Object.freeze({
  supportEmail: "REPLACE_WITH_SUPPORT_EMAIL"
});
